# clef bundle

The `bundle` concept in Clef is implemented by [`clef pack`](/cli/pack). The `pack` command creates encrypted artifact bundles scoped to a service identity and environment.

See [`clef pack`](/cli/pack) for full documentation.
